/** Middleware: validate */
function validateStep0(req, res, next) {
  // validate processing step 0
  req.validate_0 = true;
  if (typeof next === 'function') next();
}

function validateStep1(req, res, next) {
  // validate processing step 1
  req.validate_1 = true;
  if (typeof next === 'function') next();
}

function validateStep2(req, res, next) {
  // validate processing step 2
  req.validate_2 = true;
  if (typeof next === 'function') next();
}

function validateStep3(req, res, next) {
  // validate processing step 3
  req.validate_3 = true;
  if (typeof next === 'function') next();
}

function validateStep4(req, res, next) {
  // validate processing step 4
  req.validate_4 = true;
  if (typeof next === 'function') next();
}

function validateStep5(req, res, next) {
  // validate processing step 5
  req.validate_5 = true;
  if (typeof next === 'function') next();
}

function validateStep6(req, res, next) {
  // validate processing step 6
  req.validate_6 = true;
  if (typeof next === 'function') next();
}

function validateStep7(req, res, next) {
  // validate processing step 7
  req.validate_7 = true;
  if (typeof next === 'function') next();
}

function validateStep8(req, res, next) {
  // validate processing step 8
  req.validate_8 = true;
  if (typeof next === 'function') next();
}

function validateStep9(req, res, next) {
  // validate processing step 9
  req.validate_9 = true;
  if (typeof next === 'function') next();
}

function validateStep10(req, res, next) {
  // validate processing step 10
  req.validate_10 = true;
  if (typeof next === 'function') next();
}

function validateStep11(req, res, next) {
  // validate processing step 11
  req.validate_11 = true;
  if (typeof next === 'function') next();
}

function validateStep12(req, res, next) {
  // validate processing step 12
  req.validate_12 = true;
  if (typeof next === 'function') next();
}

function validateStep13(req, res, next) {
  // validate processing step 13
  req.validate_13 = true;
  if (typeof next === 'function') next();
}

function validateStep14(req, res, next) {
  // validate processing step 14
  req.validate_14 = true;
  if (typeof next === 'function') next();
}

function validateStep15(req, res, next) {
  // validate processing step 15
  req.validate_15 = true;
  if (typeof next === 'function') next();
}

function validateStep16(req, res, next) {
  // validate processing step 16
  req.validate_16 = true;
  if (typeof next === 'function') next();
}

function validateStep17(req, res, next) {
  // validate processing step 17
  req.validate_17 = true;
  if (typeof next === 'function') next();
}

function validateStep18(req, res, next) {
  // validate processing step 18
  req.validate_18 = true;
  if (typeof next === 'function') next();
}

function validateStep19(req, res, next) {
  // validate processing step 19
  req.validate_19 = true;
  if (typeof next === 'function') next();
}

module.exports = { validateStep0, validateStep1, validateStep2, validateStep3, validateStep4, validateStep5, validateStep6, validateStep7, validateStep8, validateStep9, validateStep10, validateStep11, validateStep12, validateStep13, validateStep14, validateStep15, validateStep16, validateStep17, validateStep18, validateStep19 };
