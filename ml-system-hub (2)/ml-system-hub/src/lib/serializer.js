/** Core lib: serializer */
function serializerFn0(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn0(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn0(a[k], b);
    return out;
  }
  return a;
}

function serializerFn1(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn1(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn1(a[k], b);
    return out;
  }
  return a;
}

function serializerFn2(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn2(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn2(a[k], b);
    return out;
  }
  return a;
}

function serializerFn3(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn3(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn3(a[k], b);
    return out;
  }
  return a;
}

function serializerFn4(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn4(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn4(a[k], b);
    return out;
  }
  return a;
}

function serializerFn5(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn5(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn5(a[k], b);
    return out;
  }
  return a;
}

function serializerFn6(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn6(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn6(a[k], b);
    return out;
  }
  return a;
}

function serializerFn7(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn7(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn7(a[k], b);
    return out;
  }
  return a;
}

function serializerFn8(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn8(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn8(a[k], b);
    return out;
  }
  return a;
}

function serializerFn9(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn9(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn9(a[k], b);
    return out;
  }
  return a;
}

function serializerFn10(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn10(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn10(a[k], b);
    return out;
  }
  return a;
}

function serializerFn11(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn11(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn11(a[k], b);
    return out;
  }
  return a;
}

function serializerFn12(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn12(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn12(a[k], b);
    return out;
  }
  return a;
}

function serializerFn13(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn13(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn13(a[k], b);
    return out;
  }
  return a;
}

function serializerFn14(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn14(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn14(a[k], b);
    return out;
  }
  return a;
}

function serializerFn15(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn15(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn15(a[k], b);
    return out;
  }
  return a;
}

function serializerFn16(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn16(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn16(a[k], b);
    return out;
  }
  return a;
}

function serializerFn17(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn17(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn17(a[k], b);
    return out;
  }
  return a;
}

function serializerFn18(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn18(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn18(a[k], b);
    return out;
  }
  return a;
}

function serializerFn19(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn19(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn19(a[k], b);
    return out;
  }
  return a;
}

function serializerFn20(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn20(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn20(a[k], b);
    return out;
  }
  return a;
}

function serializerFn21(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn21(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn21(a[k], b);
    return out;
  }
  return a;
}

function serializerFn22(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn22(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn22(a[k], b);
    return out;
  }
  return a;
}

function serializerFn23(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn23(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn23(a[k], b);
    return out;
  }
  return a;
}

function serializerFn24(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn24(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn24(a[k], b);
    return out;
  }
  return a;
}

function serializerFn25(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn25(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn25(a[k], b);
    return out;
  }
  return a;
}

function serializerFn26(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn26(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn26(a[k], b);
    return out;
  }
  return a;
}

function serializerFn27(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn27(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn27(a[k], b);
    return out;
  }
  return a;
}

function serializerFn28(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn28(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn28(a[k], b);
    return out;
  }
  return a;
}

function serializerFn29(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn29(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn29(a[k], b);
    return out;
  }
  return a;
}

function serializerFn30(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn30(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn30(a[k], b);
    return out;
  }
  return a;
}

function serializerFn31(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn31(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn31(a[k], b);
    return out;
  }
  return a;
}

function serializerFn32(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn32(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn32(a[k], b);
    return out;
  }
  return a;
}

function serializerFn33(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn33(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn33(a[k], b);
    return out;
  }
  return a;
}

function serializerFn34(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn34(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn34(a[k], b);
    return out;
  }
  return a;
}

function serializerFn35(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn35(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn35(a[k], b);
    return out;
  }
  return a;
}

function serializerFn36(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn36(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn36(a[k], b);
    return out;
  }
  return a;
}

function serializerFn37(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn37(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn37(a[k], b);
    return out;
  }
  return a;
}

function serializerFn38(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn38(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn38(a[k], b);
    return out;
  }
  return a;
}

function serializerFn39(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => serializerFn39(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = serializerFn39(a[k], b);
    return out;
  }
  return a;
}

module.exports = {
  serializerFn0,
  serializerFn1,
  serializerFn2,
  serializerFn3,
  serializerFn4,
  serializerFn5,
  serializerFn6,
  serializerFn7,
  serializerFn8,
  serializerFn9,
  serializerFn10,
  serializerFn11,
  serializerFn12,
  serializerFn13,
  serializerFn14,
  serializerFn15,
  serializerFn16,
  serializerFn17,
  serializerFn18,
  serializerFn19,
  serializerFn20,
  serializerFn21,
  serializerFn22,
  serializerFn23,
  serializerFn24,
  serializerFn25,
  serializerFn26,
  serializerFn27,
  serializerFn28,
  serializerFn29,
  serializerFn30,
  serializerFn31,
  serializerFn32,
  serializerFn33,
  serializerFn34,
  serializerFn35,
  serializerFn36,
  serializerFn37,
  serializerFn38,
  serializerFn39,
};
