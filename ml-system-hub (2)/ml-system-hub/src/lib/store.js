/** Core lib: store */
function storeFn0(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn0(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn0(a[k], b);
    return out;
  }
  return a;
}

function storeFn1(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn1(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn1(a[k], b);
    return out;
  }
  return a;
}

function storeFn2(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn2(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn2(a[k], b);
    return out;
  }
  return a;
}

function storeFn3(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn3(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn3(a[k], b);
    return out;
  }
  return a;
}

function storeFn4(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn4(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn4(a[k], b);
    return out;
  }
  return a;
}

function storeFn5(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn5(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn5(a[k], b);
    return out;
  }
  return a;
}

function storeFn6(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn6(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn6(a[k], b);
    return out;
  }
  return a;
}

function storeFn7(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn7(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn7(a[k], b);
    return out;
  }
  return a;
}

function storeFn8(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn8(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn8(a[k], b);
    return out;
  }
  return a;
}

function storeFn9(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn9(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn9(a[k], b);
    return out;
  }
  return a;
}

function storeFn10(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn10(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn10(a[k], b);
    return out;
  }
  return a;
}

function storeFn11(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn11(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn11(a[k], b);
    return out;
  }
  return a;
}

function storeFn12(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn12(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn12(a[k], b);
    return out;
  }
  return a;
}

function storeFn13(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn13(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn13(a[k], b);
    return out;
  }
  return a;
}

function storeFn14(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn14(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn14(a[k], b);
    return out;
  }
  return a;
}

function storeFn15(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn15(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn15(a[k], b);
    return out;
  }
  return a;
}

function storeFn16(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn16(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn16(a[k], b);
    return out;
  }
  return a;
}

function storeFn17(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn17(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn17(a[k], b);
    return out;
  }
  return a;
}

function storeFn18(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn18(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn18(a[k], b);
    return out;
  }
  return a;
}

function storeFn19(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn19(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn19(a[k], b);
    return out;
  }
  return a;
}

function storeFn20(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn20(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn20(a[k], b);
    return out;
  }
  return a;
}

function storeFn21(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn21(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn21(a[k], b);
    return out;
  }
  return a;
}

function storeFn22(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn22(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn22(a[k], b);
    return out;
  }
  return a;
}

function storeFn23(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn23(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn23(a[k], b);
    return out;
  }
  return a;
}

function storeFn24(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn24(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn24(a[k], b);
    return out;
  }
  return a;
}

function storeFn25(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn25(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn25(a[k], b);
    return out;
  }
  return a;
}

function storeFn26(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn26(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn26(a[k], b);
    return out;
  }
  return a;
}

function storeFn27(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn27(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn27(a[k], b);
    return out;
  }
  return a;
}

function storeFn28(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn28(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn28(a[k], b);
    return out;
  }
  return a;
}

function storeFn29(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn29(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn29(a[k], b);
    return out;
  }
  return a;
}

function storeFn30(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn30(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn30(a[k], b);
    return out;
  }
  return a;
}

function storeFn31(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn31(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn31(a[k], b);
    return out;
  }
  return a;
}

function storeFn32(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn32(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn32(a[k], b);
    return out;
  }
  return a;
}

function storeFn33(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn33(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn33(a[k], b);
    return out;
  }
  return a;
}

function storeFn34(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn34(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn34(a[k], b);
    return out;
  }
  return a;
}

function storeFn35(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn35(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn35(a[k], b);
    return out;
  }
  return a;
}

function storeFn36(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn36(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn36(a[k], b);
    return out;
  }
  return a;
}

function storeFn37(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn37(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn37(a[k], b);
    return out;
  }
  return a;
}

function storeFn38(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn38(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn38(a[k], b);
    return out;
  }
  return a;
}

function storeFn39(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => storeFn39(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = storeFn39(a[k], b);
    return out;
  }
  return a;
}

module.exports = {
  storeFn0,
  storeFn1,
  storeFn2,
  storeFn3,
  storeFn4,
  storeFn5,
  storeFn6,
  storeFn7,
  storeFn8,
  storeFn9,
  storeFn10,
  storeFn11,
  storeFn12,
  storeFn13,
  storeFn14,
  storeFn15,
  storeFn16,
  storeFn17,
  storeFn18,
  storeFn19,
  storeFn20,
  storeFn21,
  storeFn22,
  storeFn23,
  storeFn24,
  storeFn25,
  storeFn26,
  storeFn27,
  storeFn28,
  storeFn29,
  storeFn30,
  storeFn31,
  storeFn32,
  storeFn33,
  storeFn34,
  storeFn35,
  storeFn36,
  storeFn37,
  storeFn38,
  storeFn39,
};
