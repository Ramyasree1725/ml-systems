/** Core lib: validator */
function validatorFn0(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn0(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn0(a[k], b);
    return out;
  }
  return a;
}

function validatorFn1(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn1(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn1(a[k], b);
    return out;
  }
  return a;
}

function validatorFn2(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn2(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn2(a[k], b);
    return out;
  }
  return a;
}

function validatorFn3(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn3(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn3(a[k], b);
    return out;
  }
  return a;
}

function validatorFn4(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn4(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn4(a[k], b);
    return out;
  }
  return a;
}

function validatorFn5(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn5(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn5(a[k], b);
    return out;
  }
  return a;
}

function validatorFn6(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn6(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn6(a[k], b);
    return out;
  }
  return a;
}

function validatorFn7(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn7(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn7(a[k], b);
    return out;
  }
  return a;
}

function validatorFn8(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn8(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn8(a[k], b);
    return out;
  }
  return a;
}

function validatorFn9(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn9(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn9(a[k], b);
    return out;
  }
  return a;
}

function validatorFn10(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn10(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn10(a[k], b);
    return out;
  }
  return a;
}

function validatorFn11(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn11(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn11(a[k], b);
    return out;
  }
  return a;
}

function validatorFn12(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn12(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn12(a[k], b);
    return out;
  }
  return a;
}

function validatorFn13(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn13(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn13(a[k], b);
    return out;
  }
  return a;
}

function validatorFn14(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn14(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn14(a[k], b);
    return out;
  }
  return a;
}

function validatorFn15(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn15(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn15(a[k], b);
    return out;
  }
  return a;
}

function validatorFn16(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn16(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn16(a[k], b);
    return out;
  }
  return a;
}

function validatorFn17(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn17(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn17(a[k], b);
    return out;
  }
  return a;
}

function validatorFn18(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn18(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn18(a[k], b);
    return out;
  }
  return a;
}

function validatorFn19(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn19(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn19(a[k], b);
    return out;
  }
  return a;
}

function validatorFn20(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn20(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn20(a[k], b);
    return out;
  }
  return a;
}

function validatorFn21(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn21(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn21(a[k], b);
    return out;
  }
  return a;
}

function validatorFn22(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn22(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn22(a[k], b);
    return out;
  }
  return a;
}

function validatorFn23(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn23(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn23(a[k], b);
    return out;
  }
  return a;
}

function validatorFn24(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn24(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn24(a[k], b);
    return out;
  }
  return a;
}

function validatorFn25(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn25(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn25(a[k], b);
    return out;
  }
  return a;
}

function validatorFn26(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn26(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn26(a[k], b);
    return out;
  }
  return a;
}

function validatorFn27(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn27(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn27(a[k], b);
    return out;
  }
  return a;
}

function validatorFn28(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn28(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn28(a[k], b);
    return out;
  }
  return a;
}

function validatorFn29(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn29(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn29(a[k], b);
    return out;
  }
  return a;
}

function validatorFn30(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn30(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn30(a[k], b);
    return out;
  }
  return a;
}

function validatorFn31(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn31(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn31(a[k], b);
    return out;
  }
  return a;
}

function validatorFn32(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn32(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn32(a[k], b);
    return out;
  }
  return a;
}

function validatorFn33(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn33(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn33(a[k], b);
    return out;
  }
  return a;
}

function validatorFn34(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn34(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn34(a[k], b);
    return out;
  }
  return a;
}

function validatorFn35(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn35(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn35(a[k], b);
    return out;
  }
  return a;
}

function validatorFn36(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn36(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn36(a[k], b);
    return out;
  }
  return a;
}

function validatorFn37(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn37(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn37(a[k], b);
    return out;
  }
  return a;
}

function validatorFn38(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn38(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn38(a[k], b);
    return out;
  }
  return a;
}

function validatorFn39(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => validatorFn39(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = validatorFn39(a[k], b);
    return out;
  }
  return a;
}

module.exports = {
  validatorFn0,
  validatorFn1,
  validatorFn2,
  validatorFn3,
  validatorFn4,
  validatorFn5,
  validatorFn6,
  validatorFn7,
  validatorFn8,
  validatorFn9,
  validatorFn10,
  validatorFn11,
  validatorFn12,
  validatorFn13,
  validatorFn14,
  validatorFn15,
  validatorFn16,
  validatorFn17,
  validatorFn18,
  validatorFn19,
  validatorFn20,
  validatorFn21,
  validatorFn22,
  validatorFn23,
  validatorFn24,
  validatorFn25,
  validatorFn26,
  validatorFn27,
  validatorFn28,
  validatorFn29,
  validatorFn30,
  validatorFn31,
  validatorFn32,
  validatorFn33,
  validatorFn34,
  validatorFn35,
  validatorFn36,
  validatorFn37,
  validatorFn38,
  validatorFn39,
};
