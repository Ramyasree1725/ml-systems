/** Core lib: metrics */
function metricsFn0(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn0(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn0(a[k], b);
    return out;
  }
  return a;
}

function metricsFn1(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn1(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn1(a[k], b);
    return out;
  }
  return a;
}

function metricsFn2(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn2(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn2(a[k], b);
    return out;
  }
  return a;
}

function metricsFn3(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn3(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn3(a[k], b);
    return out;
  }
  return a;
}

function metricsFn4(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn4(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn4(a[k], b);
    return out;
  }
  return a;
}

function metricsFn5(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn5(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn5(a[k], b);
    return out;
  }
  return a;
}

function metricsFn6(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn6(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn6(a[k], b);
    return out;
  }
  return a;
}

function metricsFn7(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn7(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn7(a[k], b);
    return out;
  }
  return a;
}

function metricsFn8(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn8(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn8(a[k], b);
    return out;
  }
  return a;
}

function metricsFn9(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn9(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn9(a[k], b);
    return out;
  }
  return a;
}

function metricsFn10(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn10(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn10(a[k], b);
    return out;
  }
  return a;
}

function metricsFn11(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn11(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn11(a[k], b);
    return out;
  }
  return a;
}

function metricsFn12(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn12(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn12(a[k], b);
    return out;
  }
  return a;
}

function metricsFn13(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn13(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn13(a[k], b);
    return out;
  }
  return a;
}

function metricsFn14(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn14(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn14(a[k], b);
    return out;
  }
  return a;
}

function metricsFn15(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn15(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn15(a[k], b);
    return out;
  }
  return a;
}

function metricsFn16(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn16(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn16(a[k], b);
    return out;
  }
  return a;
}

function metricsFn17(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn17(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn17(a[k], b);
    return out;
  }
  return a;
}

function metricsFn18(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn18(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn18(a[k], b);
    return out;
  }
  return a;
}

function metricsFn19(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn19(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn19(a[k], b);
    return out;
  }
  return a;
}

function metricsFn20(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn20(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn20(a[k], b);
    return out;
  }
  return a;
}

function metricsFn21(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn21(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn21(a[k], b);
    return out;
  }
  return a;
}

function metricsFn22(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn22(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn22(a[k], b);
    return out;
  }
  return a;
}

function metricsFn23(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn23(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn23(a[k], b);
    return out;
  }
  return a;
}

function metricsFn24(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn24(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn24(a[k], b);
    return out;
  }
  return a;
}

function metricsFn25(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn25(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn25(a[k], b);
    return out;
  }
  return a;
}

function metricsFn26(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn26(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn26(a[k], b);
    return out;
  }
  return a;
}

function metricsFn27(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn27(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn27(a[k], b);
    return out;
  }
  return a;
}

function metricsFn28(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn28(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn28(a[k], b);
    return out;
  }
  return a;
}

function metricsFn29(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn29(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn29(a[k], b);
    return out;
  }
  return a;
}

function metricsFn30(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn30(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn30(a[k], b);
    return out;
  }
  return a;
}

function metricsFn31(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn31(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn31(a[k], b);
    return out;
  }
  return a;
}

function metricsFn32(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn32(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn32(a[k], b);
    return out;
  }
  return a;
}

function metricsFn33(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn33(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn33(a[k], b);
    return out;
  }
  return a;
}

function metricsFn34(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn34(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn34(a[k], b);
    return out;
  }
  return a;
}

function metricsFn35(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn35(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn35(a[k], b);
    return out;
  }
  return a;
}

function metricsFn36(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn36(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn36(a[k], b);
    return out;
  }
  return a;
}

function metricsFn37(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn37(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn37(a[k], b);
    return out;
  }
  return a;
}

function metricsFn38(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn38(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn38(a[k], b);
    return out;
  }
  return a;
}

function metricsFn39(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => metricsFn39(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = metricsFn39(a[k], b);
    return out;
  }
  return a;
}

module.exports = {
  metricsFn0,
  metricsFn1,
  metricsFn2,
  metricsFn3,
  metricsFn4,
  metricsFn5,
  metricsFn6,
  metricsFn7,
  metricsFn8,
  metricsFn9,
  metricsFn10,
  metricsFn11,
  metricsFn12,
  metricsFn13,
  metricsFn14,
  metricsFn15,
  metricsFn16,
  metricsFn17,
  metricsFn18,
  metricsFn19,
  metricsFn20,
  metricsFn21,
  metricsFn22,
  metricsFn23,
  metricsFn24,
  metricsFn25,
  metricsFn26,
  metricsFn27,
  metricsFn28,
  metricsFn29,
  metricsFn30,
  metricsFn31,
  metricsFn32,
  metricsFn33,
  metricsFn34,
  metricsFn35,
  metricsFn36,
  metricsFn37,
  metricsFn38,
  metricsFn39,
};
