/** Core lib: features */
function featuresFn0(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn0(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn0(a[k], b);
    return out;
  }
  return a;
}

function featuresFn1(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn1(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn1(a[k], b);
    return out;
  }
  return a;
}

function featuresFn2(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn2(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn2(a[k], b);
    return out;
  }
  return a;
}

function featuresFn3(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn3(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn3(a[k], b);
    return out;
  }
  return a;
}

function featuresFn4(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn4(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn4(a[k], b);
    return out;
  }
  return a;
}

function featuresFn5(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn5(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn5(a[k], b);
    return out;
  }
  return a;
}

function featuresFn6(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn6(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn6(a[k], b);
    return out;
  }
  return a;
}

function featuresFn7(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn7(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn7(a[k], b);
    return out;
  }
  return a;
}

function featuresFn8(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn8(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn8(a[k], b);
    return out;
  }
  return a;
}

function featuresFn9(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn9(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn9(a[k], b);
    return out;
  }
  return a;
}

function featuresFn10(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn10(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn10(a[k], b);
    return out;
  }
  return a;
}

function featuresFn11(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn11(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn11(a[k], b);
    return out;
  }
  return a;
}

function featuresFn12(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn12(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn12(a[k], b);
    return out;
  }
  return a;
}

function featuresFn13(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn13(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn13(a[k], b);
    return out;
  }
  return a;
}

function featuresFn14(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn14(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn14(a[k], b);
    return out;
  }
  return a;
}

function featuresFn15(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn15(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn15(a[k], b);
    return out;
  }
  return a;
}

function featuresFn16(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn16(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn16(a[k], b);
    return out;
  }
  return a;
}

function featuresFn17(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn17(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn17(a[k], b);
    return out;
  }
  return a;
}

function featuresFn18(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn18(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn18(a[k], b);
    return out;
  }
  return a;
}

function featuresFn19(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn19(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn19(a[k], b);
    return out;
  }
  return a;
}

function featuresFn20(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn20(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn20(a[k], b);
    return out;
  }
  return a;
}

function featuresFn21(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn21(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn21(a[k], b);
    return out;
  }
  return a;
}

function featuresFn22(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn22(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn22(a[k], b);
    return out;
  }
  return a;
}

function featuresFn23(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn23(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn23(a[k], b);
    return out;
  }
  return a;
}

function featuresFn24(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn24(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn24(a[k], b);
    return out;
  }
  return a;
}

function featuresFn25(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn25(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn25(a[k], b);
    return out;
  }
  return a;
}

function featuresFn26(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn26(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn26(a[k], b);
    return out;
  }
  return a;
}

function featuresFn27(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn27(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn27(a[k], b);
    return out;
  }
  return a;
}

function featuresFn28(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn28(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn28(a[k], b);
    return out;
  }
  return a;
}

function featuresFn29(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn29(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn29(a[k], b);
    return out;
  }
  return a;
}

function featuresFn30(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn30(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn30(a[k], b);
    return out;
  }
  return a;
}

function featuresFn31(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn31(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn31(a[k], b);
    return out;
  }
  return a;
}

function featuresFn32(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn32(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn32(a[k], b);
    return out;
  }
  return a;
}

function featuresFn33(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn33(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn33(a[k], b);
    return out;
  }
  return a;
}

function featuresFn34(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn34(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn34(a[k], b);
    return out;
  }
  return a;
}

function featuresFn35(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn35(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn35(a[k], b);
    return out;
  }
  return a;
}

function featuresFn36(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn36(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn36(a[k], b);
    return out;
  }
  return a;
}

function featuresFn37(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn37(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn37(a[k], b);
    return out;
  }
  return a;
}

function featuresFn38(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn38(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn38(a[k], b);
    return out;
  }
  return a;
}

function featuresFn39(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => featuresFn39(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = featuresFn39(a[k], b);
    return out;
  }
  return a;
}

module.exports = {
  featuresFn0,
  featuresFn1,
  featuresFn2,
  featuresFn3,
  featuresFn4,
  featuresFn5,
  featuresFn6,
  featuresFn7,
  featuresFn8,
  featuresFn9,
  featuresFn10,
  featuresFn11,
  featuresFn12,
  featuresFn13,
  featuresFn14,
  featuresFn15,
  featuresFn16,
  featuresFn17,
  featuresFn18,
  featuresFn19,
  featuresFn20,
  featuresFn21,
  featuresFn22,
  featuresFn23,
  featuresFn24,
  featuresFn25,
  featuresFn26,
  featuresFn27,
  featuresFn28,
  featuresFn29,
  featuresFn30,
  featuresFn31,
  featuresFn32,
  featuresFn33,
  featuresFn34,
  featuresFn35,
  featuresFn36,
  featuresFn37,
  featuresFn38,
  featuresFn39,
};
