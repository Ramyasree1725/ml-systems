/** Core lib: pipeline */
function pipelineFn0(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn0(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn0(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn1(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn1(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn1(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn2(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn2(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn2(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn3(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn3(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn3(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn4(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn4(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn4(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn5(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn5(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn5(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn6(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn6(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn6(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn7(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn7(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn7(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn8(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn8(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn8(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn9(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn9(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn9(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn10(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn10(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn10(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn11(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn11(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn11(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn12(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn12(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn12(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn13(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn13(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn13(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn14(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn14(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn14(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn15(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn15(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn15(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn16(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn16(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn16(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn17(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn17(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn17(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn18(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn18(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn18(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn19(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn19(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn19(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn20(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn20(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn20(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn21(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn21(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn21(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn22(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn22(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn22(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn23(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn23(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn23(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn24(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn24(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn24(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn25(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn25(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn25(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn26(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn26(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn26(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn27(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn27(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn27(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn28(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn28(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn28(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn29(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn29(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn29(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn30(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn30(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn30(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn31(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn31(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn31(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn32(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn32(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn32(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn33(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn33(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn33(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn34(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn34(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn34(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn35(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn35(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn35(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn36(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn36(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn36(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn37(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn37(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn37(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn38(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn38(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn38(a[k], b);
    return out;
  }
  return a;
}

function pipelineFn39(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => pipelineFn39(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = pipelineFn39(a[k], b);
    return out;
  }
  return a;
}

module.exports = {
  pipelineFn0,
  pipelineFn1,
  pipelineFn2,
  pipelineFn3,
  pipelineFn4,
  pipelineFn5,
  pipelineFn6,
  pipelineFn7,
  pipelineFn8,
  pipelineFn9,
  pipelineFn10,
  pipelineFn11,
  pipelineFn12,
  pipelineFn13,
  pipelineFn14,
  pipelineFn15,
  pipelineFn16,
  pipelineFn17,
  pipelineFn18,
  pipelineFn19,
  pipelineFn20,
  pipelineFn21,
  pipelineFn22,
  pipelineFn23,
  pipelineFn24,
  pipelineFn25,
  pipelineFn26,
  pipelineFn27,
  pipelineFn28,
  pipelineFn29,
  pipelineFn30,
  pipelineFn31,
  pipelineFn32,
  pipelineFn33,
  pipelineFn34,
  pipelineFn35,
  pipelineFn36,
  pipelineFn37,
  pipelineFn38,
  pipelineFn39,
};
