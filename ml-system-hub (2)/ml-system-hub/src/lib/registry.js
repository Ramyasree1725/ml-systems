/** Core lib: registry */
function registryFn0(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn0(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn0(a[k], b);
    return out;
  }
  return a;
}

function registryFn1(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn1(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn1(a[k], b);
    return out;
  }
  return a;
}

function registryFn2(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn2(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn2(a[k], b);
    return out;
  }
  return a;
}

function registryFn3(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn3(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn3(a[k], b);
    return out;
  }
  return a;
}

function registryFn4(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn4(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn4(a[k], b);
    return out;
  }
  return a;
}

function registryFn5(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn5(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn5(a[k], b);
    return out;
  }
  return a;
}

function registryFn6(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn6(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn6(a[k], b);
    return out;
  }
  return a;
}

function registryFn7(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn7(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn7(a[k], b);
    return out;
  }
  return a;
}

function registryFn8(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn8(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn8(a[k], b);
    return out;
  }
  return a;
}

function registryFn9(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn9(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn9(a[k], b);
    return out;
  }
  return a;
}

function registryFn10(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn10(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn10(a[k], b);
    return out;
  }
  return a;
}

function registryFn11(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn11(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn11(a[k], b);
    return out;
  }
  return a;
}

function registryFn12(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn12(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn12(a[k], b);
    return out;
  }
  return a;
}

function registryFn13(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn13(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn13(a[k], b);
    return out;
  }
  return a;
}

function registryFn14(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn14(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn14(a[k], b);
    return out;
  }
  return a;
}

function registryFn15(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn15(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn15(a[k], b);
    return out;
  }
  return a;
}

function registryFn16(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn16(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn16(a[k], b);
    return out;
  }
  return a;
}

function registryFn17(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn17(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn17(a[k], b);
    return out;
  }
  return a;
}

function registryFn18(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn18(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn18(a[k], b);
    return out;
  }
  return a;
}

function registryFn19(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn19(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn19(a[k], b);
    return out;
  }
  return a;
}

function registryFn20(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn20(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn20(a[k], b);
    return out;
  }
  return a;
}

function registryFn21(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn21(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn21(a[k], b);
    return out;
  }
  return a;
}

function registryFn22(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn22(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn22(a[k], b);
    return out;
  }
  return a;
}

function registryFn23(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn23(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn23(a[k], b);
    return out;
  }
  return a;
}

function registryFn24(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn24(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn24(a[k], b);
    return out;
  }
  return a;
}

function registryFn25(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn25(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn25(a[k], b);
    return out;
  }
  return a;
}

function registryFn26(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn26(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn26(a[k], b);
    return out;
  }
  return a;
}

function registryFn27(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn27(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn27(a[k], b);
    return out;
  }
  return a;
}

function registryFn28(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn28(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn28(a[k], b);
    return out;
  }
  return a;
}

function registryFn29(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn29(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn29(a[k], b);
    return out;
  }
  return a;
}

function registryFn30(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn30(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn30(a[k], b);
    return out;
  }
  return a;
}

function registryFn31(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn31(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn31(a[k], b);
    return out;
  }
  return a;
}

function registryFn32(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn32(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn32(a[k], b);
    return out;
  }
  return a;
}

function registryFn33(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn33(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn33(a[k], b);
    return out;
  }
  return a;
}

function registryFn34(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn34(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn34(a[k], b);
    return out;
  }
  return a;
}

function registryFn35(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn35(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn35(a[k], b);
    return out;
  }
  return a;
}

function registryFn36(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn36(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn36(a[k], b);
    return out;
  }
  return a;
}

function registryFn37(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn37(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn37(a[k], b);
    return out;
  }
  return a;
}

function registryFn38(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn38(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn38(a[k], b);
    return out;
  }
  return a;
}

function registryFn39(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => registryFn39(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = registryFn39(a[k], b);
    return out;
  }
  return a;
}

module.exports = {
  registryFn0,
  registryFn1,
  registryFn2,
  registryFn3,
  registryFn4,
  registryFn5,
  registryFn6,
  registryFn7,
  registryFn8,
  registryFn9,
  registryFn10,
  registryFn11,
  registryFn12,
  registryFn13,
  registryFn14,
  registryFn15,
  registryFn16,
  registryFn17,
  registryFn18,
  registryFn19,
  registryFn20,
  registryFn21,
  registryFn22,
  registryFn23,
  registryFn24,
  registryFn25,
  registryFn26,
  registryFn27,
  registryFn28,
  registryFn29,
  registryFn30,
  registryFn31,
  registryFn32,
  registryFn33,
  registryFn34,
  registryFn35,
  registryFn36,
  registryFn37,
  registryFn38,
  registryFn39,
};
