/** Core lib: cache */
function cacheFn0(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn0(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn0(a[k], b);
    return out;
  }
  return a;
}

function cacheFn1(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn1(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn1(a[k], b);
    return out;
  }
  return a;
}

function cacheFn2(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn2(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn2(a[k], b);
    return out;
  }
  return a;
}

function cacheFn3(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn3(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn3(a[k], b);
    return out;
  }
  return a;
}

function cacheFn4(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn4(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn4(a[k], b);
    return out;
  }
  return a;
}

function cacheFn5(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn5(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn5(a[k], b);
    return out;
  }
  return a;
}

function cacheFn6(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn6(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn6(a[k], b);
    return out;
  }
  return a;
}

function cacheFn7(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn7(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn7(a[k], b);
    return out;
  }
  return a;
}

function cacheFn8(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn8(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn8(a[k], b);
    return out;
  }
  return a;
}

function cacheFn9(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn9(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn9(a[k], b);
    return out;
  }
  return a;
}

function cacheFn10(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn10(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn10(a[k], b);
    return out;
  }
  return a;
}

function cacheFn11(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn11(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn11(a[k], b);
    return out;
  }
  return a;
}

function cacheFn12(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn12(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn12(a[k], b);
    return out;
  }
  return a;
}

function cacheFn13(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn13(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn13(a[k], b);
    return out;
  }
  return a;
}

function cacheFn14(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn14(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn14(a[k], b);
    return out;
  }
  return a;
}

function cacheFn15(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn15(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn15(a[k], b);
    return out;
  }
  return a;
}

function cacheFn16(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn16(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn16(a[k], b);
    return out;
  }
  return a;
}

function cacheFn17(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn17(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn17(a[k], b);
    return out;
  }
  return a;
}

function cacheFn18(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn18(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn18(a[k], b);
    return out;
  }
  return a;
}

function cacheFn19(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn19(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn19(a[k], b);
    return out;
  }
  return a;
}

function cacheFn20(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn20(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn20(a[k], b);
    return out;
  }
  return a;
}

function cacheFn21(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn21(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn21(a[k], b);
    return out;
  }
  return a;
}

function cacheFn22(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn22(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn22(a[k], b);
    return out;
  }
  return a;
}

function cacheFn23(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn23(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn23(a[k], b);
    return out;
  }
  return a;
}

function cacheFn24(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn24(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn24(a[k], b);
    return out;
  }
  return a;
}

function cacheFn25(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn25(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn25(a[k], b);
    return out;
  }
  return a;
}

function cacheFn26(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn26(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn26(a[k], b);
    return out;
  }
  return a;
}

function cacheFn27(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn27(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn27(a[k], b);
    return out;
  }
  return a;
}

function cacheFn28(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn28(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn28(a[k], b);
    return out;
  }
  return a;
}

function cacheFn29(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn29(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn29(a[k], b);
    return out;
  }
  return a;
}

function cacheFn30(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn30(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn30(a[k], b);
    return out;
  }
  return a;
}

function cacheFn31(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn31(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn31(a[k], b);
    return out;
  }
  return a;
}

function cacheFn32(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn32(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn32(a[k], b);
    return out;
  }
  return a;
}

function cacheFn33(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn33(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn33(a[k], b);
    return out;
  }
  return a;
}

function cacheFn34(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn34(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn34(a[k], b);
    return out;
  }
  return a;
}

function cacheFn35(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn35(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn35(a[k], b);
    return out;
  }
  return a;
}

function cacheFn36(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn36(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn36(a[k], b);
    return out;
  }
  return a;
}

function cacheFn37(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn37(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn37(a[k], b);
    return out;
  }
  return a;
}

function cacheFn38(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn38(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn38(a[k], b);
    return out;
  }
  return a;
}

function cacheFn39(a, b = null) {
  if (a == null) return b;
  if (Array.isArray(a)) return a.map(x => cacheFn39(x, b));
  if (typeof a === 'object') {
    const out = {};
    for (const k of Object.keys(a)) out[k] = cacheFn39(a[k], b);
    return out;
  }
  return a;
}

module.exports = {
  cacheFn0,
  cacheFn1,
  cacheFn2,
  cacheFn3,
  cacheFn4,
  cacheFn5,
  cacheFn6,
  cacheFn7,
  cacheFn8,
  cacheFn9,
  cacheFn10,
  cacheFn11,
  cacheFn12,
  cacheFn13,
  cacheFn14,
  cacheFn15,
  cacheFn16,
  cacheFn17,
  cacheFn18,
  cacheFn19,
  cacheFn20,
  cacheFn21,
  cacheFn22,
  cacheFn23,
  cacheFn24,
  cacheFn25,
  cacheFn26,
  cacheFn27,
  cacheFn28,
  cacheFn29,
  cacheFn30,
  cacheFn31,
  cacheFn32,
  cacheFn33,
  cacheFn34,
  cacheFn35,
  cacheFn36,
  cacheFn37,
  cacheFn38,
  cacheFn39,
};
